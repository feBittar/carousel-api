import { ModuleData, RenderContext } from '../types';
/**
 * Generates HTML for the Free Image module
 *
 * Renders an absolutely positioned image at the center of the carousel.
 * Only shows when enabled and URL is provided.
 */
export declare function getFreeImageHtml(data: ModuleData, context?: RenderContext): string;
//# sourceMappingURL=html.d.ts.map