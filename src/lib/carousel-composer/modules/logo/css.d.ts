import { ModuleData, RenderContext } from '../types';
/**
 * Generates CSS for the Logo Module
 */
export declare function getLogoCss(data: ModuleData, context?: RenderContext): string;
/**
 * Generates CSS variables for the Logo Module (for compatibility with legacy templates)
 */
export declare function getLogoStyleVariables(data: ModuleData): Record<string, string>;
//# sourceMappingURL=css.d.ts.map